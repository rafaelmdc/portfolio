# Base URL of the headless Wagtail backend (server-side fetches only).
# Local dev (docker compose stack on :8000):
WAGTAIL_API_URL=http://localhost:8000

# In production this points at the internal backend Service, e.g.:
# WAGTAIL_API_URL=http://portfolio-backend:8000
